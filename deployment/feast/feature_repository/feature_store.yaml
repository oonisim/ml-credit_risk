project: customer_credit_risk
registry:
    registry_type: sql
    path: postgresql+psycopg2://postgres@localhost:5432/feast_registry
    cache_ttl_seconds: 60
    #sqlalchemy_config_kwargs:
    #    echo: True               # To show verbose SQLAlchemy operations.
    #    pool_pre_ping: true
    #  connect_args:
    #    timeout: 30
    #    check_same_thread: False

provider: local
offline_store:
  type: postgres
  host: localhost
  port: 5432
  database: offline_features
  db_schema: credit
  user: dbadm
  password: P@ssword

online_store:
  type: sqlite
  path: data/online_store.db

entity_key_serialization_version: 3
# By default, no_auth for authentication and authorization,
# other possible values kubernetes and oidc.
# Refer the documentation for more details.

auth:
  type: no_auth